---
description: Two modes. **Produce mode**: generates a self-contained "Brief:" document for a cloud / remote / async agent that runs in a sandbox without conversation history. Triggers on "cloud agent", "remote agent", "hand off", "handoff", "prime", "brief", "draft a prime", "give me the brief", "ship to cloud", "primer", "let me give it to a cloud agent", or any time the user describes a task they want to dispatch elsewhere instead of running locally. Produces the "Brief:" document with repo-relative paths only. **Receive mode**: when the user pastes a cloud agent's report (phrases like "summary below", "## Commits", "PR ready", "branch pushed", "Closes #", "## Deviations", commit hash + subject tables, or the literal string "Done. Summary below."), run the post-receipt audit to scrub AI-attribution trailers ("Generated by [Claude Code]", "Co-Authored-By: Claude", session URLs) that cloud agents consistently slip into PR bodies despite the brief forbidding them — do NOT trust the agent's self-report of "no markers".
---

You are producing a **cloud-agent prime** in the established `Brief:` format. The output is a single self-contained markdown document the user copies and pastes into a cloud agent (Claude Cloud Agents, Codex-style remote runner, etc.). The agent has zero conversation history and runs **in the repo sandbox** — not on the local machine.

## Hard rules

1. **No absolute paths.** Never reference a local-machine path — `/home/<user>/projects/<repo>`, `/workspace/<repo>`, a Windows drive path like `C:\Users\...`, or anything else tied to your machine. The agent has no idea where the repo lives in its sandbox. All paths must be repo-relative (`packages/engine/src/scan/walker.ts`).
2. **No local-only verification steps.** Things that need a local test drive, local devices, or the user's actual environment can't go in "verification before declaring done" — they belong in a separate **Local review steps** section that the user runs after the agent reports back.
3. **Self-contained.** The agent has no chat history, no scrollback, no memory of prior conversations. Anything it needs to know — file paths, schema shapes, naming conventions, gotchas, ESLint rules already in place — must be in the brief itself or be discoverable via `gh issue view`, `git log`, or reading the repo.
4. **No commit-history breadcrumbs about meta-tooling, the cloud-vs-local handoff, or implementation scaffolding.** Commits and PR bodies should read as if a human wrote them.
5. **No AI-attribution trailers anywhere that lands in source control.** The brief MUST explicitly instruct the cloud agent: do not add `Generated by [Claude Code]` lines, `claude.ai/code/session_...` URLs, `Co-Authored-By: Claude ...` trailers, `🤖 Generated with ...` markers, or any similar generator/agent attribution to commit messages OR PR descriptions. PR bodies and commit messages can both end up in `git log` on `main` depending on the merge strategy — anything in either lands permanently. Put this as an explicit non-goal in every brief.

## Format (match this exactly)

```markdown
# Brief: <terse description of the task>

<1-3 sentence opening paragraph: project state, what's being addressed, optionally why now>

## First thing this session

```
git config user.email "<operator-email>" && git config user.name "<operator-name>"
```

<Authoring note — do NOT leave the angle-bracket placeholders in the emitted brief. At generation time, read the operator's real local identity and substitute it:
  git config user.email
  git config user.name
Inject those literal values into the line above so the cloud agent commits under the correct identity. If either is unset, leave the explicit placeholder `<operator-email>` / `<operator-name>` in place so the operator notices and fills it in before dispatching — never invent or hardcode an address.>

<Brief 1-2 sentences on branch policy: work on a feature branch the agent creates (cloud agents default to `claude/<slug>-XXXXX` — that's fine), one commit per task in order, push the branch and open a PR back to `main` when done. Closing keywords on commits will auto-close issues when the PR is merged (the user merges, not the agent).>

## Tasks

### T01 — <imperative verb + scope>
<Goal in 1-2 sentences>

**Files:** `<path>`, `<path>`

**Implementation:**
- <bullet>
- <bullet>

**Test:**
- <vitest / jest / etc. test sketch>

**Commit:** `<conventional-commit subject>`

### T02 — <…>
<…>

<Repeat for each task>

## Important context (read before starting)

- **<gotcha 1>** — <one-line explanation with file path>
- **<gotcha 2>** — <one-line explanation>
- <existing patterns to mirror: file paths, naming conventions, the lint rule already in place, etc.>

## Non-goals

- Do not <X>.
- Do not <Y>.

## Verification before declaring done

1. All <N> commits present on the feature branch, pushed to origin.
2. PR opened against `main` from that branch (the agent opens it; the user merges).
3. `gh run list --limit 3` shows the latest CI run on the branch tip is **green** (if the repo has CI).
4. Issue #<n> will close automatically by the closing keyword in the matching commit when the PR is merged — confirm the keyword is in the commit subject.

## Reporting back

Post a summary in the same shape as past milestone reports — table of commits with hashes, summary of behavior change, test count delta, and any deviations from the plan. Flag anything you couldn't meet so the local reviewer can decide whether to redirect or accept.

Go.
```

## How to write each section

- **Opening paragraph.** State the project version / phase if relevant ("v0.1.0, post-beta"), the immediate problem, and the connecting tissue between tasks. Don't summarize the task list — the task list speaks for itself.
- **Tasks.** Each task is independent enough to land as its own commit. Order matters when later tasks depend on earlier scaffolding (migrations before repos before usage). Use `T01`, `T02`, … numbering.
- **Files.** List the exact repo-relative file paths the agent will touch. If a new file is being created, say `(new)` after the path.
- **Test.** Sketch the test, don't write it. The agent fills in the details. Specify what should be asserted, not how.
- **Commit subject.** Use the project's conventional-commit style (look at recent `git log --oneline` for the pattern: `feat(scope): …`, `fix(scope): …`, `perf+arch(scope): …`, `chore(scope): …`).
- **Important context.** This is where existing schema constraints, lint rules, naming conventions, in-process state assumptions, and "things you'd otherwise miss" live. Reference exact file paths so the agent can read them directly.
- **Non-goals.** Things the agent might be tempted to also fix or refactor while in there. Explicitly call them out so it doesn't expand scope.

## Common mistakes (from past handoffs)

- Putting a local absolute path (e.g. `/home/<user>/projects/<repo>`) in verification → the agent's sandbox has no such path.
- Saying "the user's local repo" → repo IS the agent's working directory.
- Telling the agent to "push directly to main" → cloud agents push to a sandbox feature branch by default and ignore main-only directives. Honor the natural flow: feature branch + PR.
- Forgetting `git config user.email / user.name` → commits land with the wrong author.
- Forgetting the closing-keyword on the final commit (`closes #N`) → issue stays open.
- Letting the agent add a `Generated by [Claude Code](claude.ai/code/session_...)` trailer or `Co-Authored-By: Claude` line to the PR body or commit message → these end up in the squash-merge commit and live forever in `git log` on a public repo. Always put a non-goal in the brief forbidding these.

## Output

Save the brief to `~/.claude/plans/<YYYY-MM-DD>-<slug>-cloud-prime.md` and print it in a fenced markdown code block in your response so the user can copy from chat. End your response with the file path so they know where it lives.

The saved file is the primary handoff artifact — the user can open it, copy from it, or attach it directly. After printing the brief, offer to copy it to the clipboard if a clipboard utility is available in the environment (e.g. `xclip`/`xsel`/`wl-copy` on Linux, `pbcopy` on macOS, or whatever bridge the harness exposes). If none is available, point the user at the saved file path — that's sufficient. Don't assume any particular OS or shell. If they decline, leave it.

**Do not** dispatch the brief — that's the user's step. **Do not** start implementing — this skill produces the prime, nothing else.

## Post-receipt audit (receive mode)

Cloud agents consistently slip AI-attribution trailers into PR bodies despite the brief's explicit non-goal. This recurs even when the agent's own self-report claims "no AI-attribution markers" — the markers are there in the PR body anyway. **Do not trust the agent's self-report.** The non-goal in the produced brief is advisory at best — treat the audit as enforcement.

When the user pastes a cloud-agent report (commit hash table, "PR opened at #N", "summary below", "Closes #N", "done"), **before** suggesting merge:

1. **Identify the PR.** From the user's message, or via `gh pr list --repo <owner>/<repo> --state open --limit 3`. Pick the most recent PR matching the work described.

2. **Fetch PR body + commit metadata:**

   ```
   gh pr view <num> --repo <owner>/<repo> --json body,commits,additions,deletions,files --jq '{body: .body, commits: [.commits[] | {sha: .oid[0:7], msg: .messageHeadline, body: .messageBody}], files: [.files[].path], additions: .additions, deletions: .deletions}'
   ```

3. **Fetch source diff** for any non-trivial changes (in case markers slipped into code, comments, or generated docs):

   ```
   # resolve the branch head SHA, then fetch each file at that revision
   sha=$(gh pr view <num> --repo <owner>/<repo> --json headRefOid --jq .headRefOid)
   gh api "repos/<owner>/<repo>/contents/<path>?ref=$sha" --jq '.content' | base64 -d
   ```

   `gh api` keeps it authenticated and avoids reaching for a raw HTTP client; iterate over the file list from step 2.

4. **Scan patterns (case-insensitive):**
   - `claude\.ai/code`
   - `Generated by\s*\[?Claude`
   - `Co-Authored-By:\s*Claude`
   - `🤖\s*Generated with`
   - `session_[a-zA-Z0-9]{16,}`
   - `Anthropic\b` (in commit / PR-body context)

5. **Remediation by location:**

   - **PR body** (the common case): rewrite via `gh pr edit <num> --body-file <clean.md>`. Write a clean version of the body to a temp markdown file (drop the trailing `---` separator + the `_Generated by [Claude Code](...)_` line, usually the last 2–3 lines), then apply. Verify with another `gh pr view --json body` afterwards.

   - **Commit messages**: rewriting is destructive (changes hashes). Flag to the user; usually the right call is to either (a) accept and squash-merge so only the squash subject lands on `main`, or (b) ask the cloud agent to amend and force-push the branch. Don't rewrite without explicit consent.

   - **Source files**: rare so far. Edit + commit a follow-up on `main` after merge, or have the cloud agent fix on the branch before merge. Surface to user before suggesting merge.

6. **Always report what was found and what was stripped.** Don't silently scrub. The user wants to know the agent slipped a trailer so they can factor it into future cloud handoffs.

The brief still forbids the trailer (it might catch a future, better-behaved agent). The audit is the enforcement layer.
